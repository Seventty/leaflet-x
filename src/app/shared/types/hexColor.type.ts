

/**
 * Represents a hexadecimal color in '#RRGGBB' format where RR, GG and BB are components
 * red, green and blue respectively, each represented by two hexadecimal digits.
 *
 * example: #fffff
 * @type {`#${string}`} HexColor
*/
export type HexColorType = `#${string}`;
